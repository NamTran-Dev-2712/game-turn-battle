# `build/` — Output build [generated]

Thư mục **sinh tự động** cho output build (export client, publish server…). **Gitignored** — chỉ giữ README này. Không commit artifact. Xem `../docs/deployment/`.
