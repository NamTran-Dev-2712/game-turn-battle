# `.github/workflows/` — GitHub Actions

`ci-server.yml`, `ci-client.yml`, `validate-config.yml`, `release.yml`. Trigger & nội dung theo [`../../docs/deployment/ci-cd-pipeline.md`](../../docs/deployment/ci-cd-pipeline.md). **Bootstrap:** skeleton; bước nặng (Godot export, config-validator, golden vector, push registry) là TODO. Không đặt secret ở đây.
