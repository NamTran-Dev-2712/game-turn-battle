# Workflow: Documentation Sync (Mandatory Update Policy)

> **Rule:** documentation must never lag implementation. Any change that alters architecture,
> dependencies, config schema, public API/contract, a module boundary, or public behavior **must**
> update the affected docs **in the same change**. A change that skips this is **not Done**
> (`docs/ai/review-and-dod.md` §4.5).
>
> **Principle:** *index, don't repeat.* Update the **canonical** doc; make derived docs *link* it,
> never copy. `docs/` is the SSOT — this layer keeps it honest, it does not replace it.

## How to use
1. Run `git diff` and classify the change against the matrix below.
2. For every matching row, open the listed docs and update them before declaring Done.
3. If a change would contradict `docs/mvp/` or an Accepted ADR: **stop** — that's a code defect or a
   new ADR is required. Do not edit the SSOT to match the code.

## Change → Doc-impact matrix

| If the change… | Update (canonical first) |
|---|---|
| Makes a new architectural decision | New ADR (`docs/adr/` template) → index in `DECISIONS.md` + ADR catalog (`docs/adr/README.md`) → foundational-decisions table if applicable |
| Reverses/supersedes a decision | Mark old ADR `Superseded`, add new ADR, fix cross-refs |
| Adds/removes a dependency (NuGet/addon) | ADR-010 rationale (in PR) + `server/Directory.Packages.props` (or Godot addon note in `docs/godot/tooling-and-testing.md`) |
| Changes the config schema | `shared/config-schema/*.schema.json` (per-type + `common.schema.json` + `config-bundle.schema.json`) + matching `fixtures/*.{valid,invalid}.json` + `docs/liveops/remote-config.md` + `docs/gameplay/configuration-and-data.md` (schema-mapping table) + `docs/conventions/data-and-docs-conventions.md` (ID-prefix table) + `config/*/README.md` if a domain is affected. **Breaking change** (remove/rename field, tighten type, narrow enum, change meaning) → bump `schema_version` + add migration under `shared/config-schema/_versions/` (see its README). Never hardcode balance in a schema. Cross-file referential integrity is validated by `tools/config-validator` (phase-07 gate), not JSON Schema alone — **run it** (`bash tools/config-validator/run.sh config shared/config-schema`) and update its tests when a new config type/ref is added. |
| Changes the config validator (behavior / error codes / refs) | `tools/config-validator` (core lib + xUnit tests — the behavior contract) + `tools/config-validator/README.md` (error-code table `JSON001…REF002`) + `.github/workflows/validate-config.yml` (mandatory gate) + `docs/gameplay/configuration-and-data.md` + `docs/deployment/ci-cd-pipeline.md` + `.instructions/config.md`. A new config type MUST add schema + validator mapping + refs + test together. **Never** bypass/weaken the gate to make CI green; fix the correct layer. Config Service (phase 21) reuses the core — do not fork a second validator. |
| Changes a client↔server contract/DTO | `server/GameTeam.Contracts` (one public type/file, additive-only) → **rebuild** to regenerate `shared/contracts/openapi.json` (never hand-edit; CI drift-guard enforces) + `docs/backend/api-and-versioning.md`; **regenerate client models** `bash shared/codegen/run.sh` → **commit** the diff in `client/src/data/generated/` (never hand-edit; `codegen-check.yml` drift gate enforces). **Enum change:** keep numeric values stable (add-only, deprecate not reuse), update `EnumStabilityTests` deliberately (`server/tests/GameTeam.Contracts.Tests`); enums flow to GDScript via `x-enum-varnames`/`x-enum-values` (`ContractEnumsDocumentTransformer`) |
| Changes an Application pipeline behavior / port | `server/src/GameTeam.Application/Behaviors/*` + ports (`Abstractions/*`) + markers + `AddApplication` order + `GameTeam.Application.Tests` (behavior tests + `PipelineOrderTests` + `ArchitectureTests` — the contract) + `docs/backend/cross-cutting.md` §2.5 + `docs/backend/domain-and-application.md` §2 + `.instructions/backend.md` + `.claude/agents/dotnet-backend.md`. Fixed order **Logging → Validation → Transaction → Caching**; cross-cutting lives in behaviors (never handlers); transaction only on `ITransactionalRequest`, cache only on `ICacheableQuery`; `GameTeam.Application` must not reference Infrastructure/Api. |
| Changes persistence (EF Core / DbContext / repository / UoW / migration / domain-event dispatch) | `server/src/GameTeam.Infrastructure/Persistence/*` (`AppDbContext`, `EfRepository`, `UnitOfWork`, `DomainEventDispatcher`, `Configurations/*`, `Migrations/*`) + `GameTeam.Infrastructure.Tests/Persistence/*` (Testcontainers integration — the behavior contract) + the EF-boundary `ArchitectureTests` fact + `docs/backend/infrastructure.md` §1.1/§5.1 + `docs/testing/backend-testing.md` §4.1 + `docs/backend/domain-and-application.md` (dispatch lifecycle) + `.instructions/backend.md` + `.claude/agents/dotnet-backend.md`. EF/Npgsql **only** in Infrastructure (never leak `IQueryable`/`DbContext` up); connection string from config `ConnectionStrings:Postgres` (never hardcode); schema changes go through **migrations** (`HasData` for seed; `has-pending-model-changes` clean) — never hand-edit the DB; domain events dispatch **only** at `AppDbContext.SaveChangesAsync` (after persist, same transaction). New EF package → `Directory.Packages.props` (CPM). Reuse the DbContext/UoW/repo/dispatcher — do not fork a second one. |
| Changes caching (Redis / `ICacheService` / `RedisCacheService` / key convention / serialization) | `server/src/GameTeam.Infrastructure/Caching/*` (`RedisCacheService`, `RedisCacheKey`) + `Serialization/*` (`ResultJsonConverterFactory`, `CacheSerialization`) + `ICacheService` port (`Application/Abstractions/Caching`) + `GameTeam.Infrastructure.Tests/Caching/*` (Testcontainers Redis — the behavior contract) + `AddInfrastructure` DI + `/health` (Api) + `docs/backend/infrastructure.md` §2.1 + `docs/backend/cross-cutting.md` §2.5/§4 + `.instructions/backend.md` + `.claude/agents/dotnet-backend.md`. StackExchange.Redis **only** in Infrastructure (never leak Redis types up, never bypass `ICacheService`); connection from config `ConnectionStrings:Redis` (never hardcode); **graceful degradation** (Redis errors log + degrade, never crash a request); key `{env}:{domain}:{name}:{configVersion?}` (config-version invalidation, ADR-005); TTL is absolute expiry. New Redis package → `Directory.Packages.props` (CPM). Reuse the service — do not fork a second cache. Config Service (phase 21) reuses it. |
| Changes auth (JWT / `ITokenService` / `Account` / authorization policy / public whitelist) | `server/src/GameTeam.Domain/Accounts/*` (`Account`/`AccountType`/`AccountCreated`) + `server/src/GameTeam.Application/Abstractions/Security/ITokenService.cs` + `Features/Auth/*` + `server/src/GameTeam.Infrastructure/Auth/*` (`JwtTokenService`/`JwtOptions`) + `Persistence/Configurations/AccountConfiguration.cs` + `Persistence/Migrations/*` + `server/src/GameTeam.Api/DependencyInjection.cs` (scheme + `FallbackPolicy`) + `Program.cs` (`UseAuthentication/UseAuthorization` + `.AllowAnonymous` whitelist) + `Http/AuthProblem.cs` + tests (`AuthGuestEndpointTests`, `CreateGuestAccountCommandTests`, `AccountTests`, `AccountPersistenceTests`, `JwtTokenServiceTests`, the `Application_should_not_depend_on_jwt_or_authentication_frameworks` arch fact — the behavior contract) + `docs/backend/api-and-versioning.md` §4.5 + `docs/backend/infrastructure.md` §2.5 + `docs/backend/cross-cutting.md` §1 + `.instructions/backend.md` + `.claude/agents/dotnet-backend.md`. **JWT lives only in Infrastructure** (Application depends on `ITokenService`, never a JWT framework — arch gate); **business endpoints authenticated by default** (`FallbackPolicy=RequireAuthenticatedUser`), public endpoints **explicitly** `.AllowAnonymous()` (health/auth/openapi/swagger). **Signing key from secret/env `Jwt__SigningKey`** (`IOptions<JwtOptions>`, fail-fast) — **never hardcode/commit/log**; appsettings holds only issuer/audience/expiry. Auth errors → `ErrorEnvelope` (401 `UNAUTHENTICATED`/403 `FORBIDDEN`). New JWT package → `Directory.Packages.props` (CPM). Provider linking = Post-MVP (future `account_providers` table). Reuse this auth infra — never a second mechanism, never fake a user, never bypass authz. |
| Changes the player profile / save root (`PlayerProfile` / schema versioning / ownership) | `server/src/GameTeam.Domain/Profiles/*` (`PlayerProfile`/`PlayerProfileCreated`; `CurrentSchemaVersion` + `Upgrade` migration chain) + `server/src/GameTeam.Application/Features/Profile/*` (`GetOrCreateProfileCommand`/`GetMyProfileQuery` + handlers + `ProfileErrors`/`ProfileMapping`) + `Abstractions/Security/ICurrentUser.cs` + `Abstractions/Persistence/IPlayerProfileRepository.cs` + `server/src/GameTeam.Api/Auth/CurrentUser.cs` + `GameTeam.Api/DependencyInjection.cs` (`AddHttpContextAccessor` + `ICurrentUser`) + `Program.cs` (`/profile` in the **version set**, protected) + `server/src/GameTeam.Infrastructure/Persistence/Configurations/PlayerProfileConfiguration.cs` + `Repositories/PlayerProfileRepository.cs` + `AppDbContext` (`DbSet`) + `Persistence/Migrations/*_AddPlayerProfiles.cs` + `CreateGuestAccountCommandHandler` (eager profile) + tests (`PlayerProfileTests`, `GetOrCreateProfileCommandHandlerTests`, `GetMyProfileQueryHandlerTests`, `PlayerProfilePersistenceTests`, `ProfileEndpointTests` — the behavior contract) + `docs/backend/domain-and-application.md` + `docs/backend/infrastructure.md` §1.2 + `docs/adr/ADR-007-save-strategy.md` (Implementation) + `.instructions/backend.md` + `.claude/agents/dotnet-backend.md`. **`PlayerProfile` is the server-authoritative save root** (ADR-007) — future feature state **extends** it (never a parallel root). **Ownership resolved ONLY from token `sub`** via `ICurrentUser` — never a client-supplied id (IDOR). **Idempotency = unique `account_id` index** (never check-then-insert); guest login creates account+profile in one transaction. **Any profile-schema change ⇒ bump `SchemaVersion` + a `MigrateV{n}ToV{n+1}` step + a data-preservation test + an EF migration** (profile **data** migration is distinct from EF DDL migration). All mutation via a server command (client sets nothing). |
| Changes a client core autoload / EventBus event / scene navigation | `client/src/core/events/*` or `client/src/core/scene/*` + `client/tests/core/*` (behavior contract) + `client/project.godot` (`[autoload]`) + `docs/godot/state-and-signals.md` §3.1 (EventBus **event catalogue** — every event a declared `EVENTS`+`signal` entry, documented; no "God channel") + `docs/godot/scene-architecture.md` §4.1 (SceneRouter API/lifecycle) + `.instructions/client.md` + `.claude/agents/godot-client.md`. Two **independent** autoloads (no God autoload); features never navigate ad-hoc or import each other — EventBus/SceneRouter only (ADR-002). Autoload scripts omit `class_name`. |
| Changes the client network transport (`NetworkClient` / `client/src/core/net/*`) | `client/src/core/net/*` (`network_client.gd` + `HttpTransport`/`GodotHttpTransport`/`TokenStore`/`NetResult`/`NetworkResponseParser`) + `client/tests/core/net/*` (gdUnit4 with `FakeHttpTransport` — the behavior contract) + `client/project.godot` (`[autoload]`) + any new EventBus event (`network_error`/`unauthorized` → `EventBus.EVENTS` + `signal` + `docs/godot/state-and-signals.md` §3.1 catalogue) + `docs/godot/state-and-signals.md` §4 (NetworkClient) + `docs/godot/ui-architecture.md` §1 (UI must not call net) + `client/src/core/net/README.md` + `.instructions/client.md` + `.claude/agents/godot-client.md`. **Single gateway** — `HTTPRequest` only in `core/net/` (grep guard); UI/feature never call HTTP directly. Responses parse into **generated models** via `NetworkResponseParser` (never hand-declare a DTO / hand-edit generated). Retry **GET only**; POST never auto-retried. **Never log token/Authorization; never hardcode a token.** Network loss → report failure, **never fabricate** a result (ADR-008/011). Reuse — do not fork a second HTTP client. |
| Changes the client config/state cache (`ConfigProvider` / `StateCache` / `client/src/core/{config,state}/*`) | `client/src/core/config/*` (`config_provider.gd`) + `client/src/core/state/*` (`state_cache.gd`) + `client/tests/core/{config,state}/*` (gdUnit4 — the behavior contract) + `client/project.godot` (`[autoload]`, after `NetworkClient`) + any new EventBus event (`config_updated`/`state_refreshed` → `EventBus.EVENTS` + `signal` + `docs/godot/state-and-signals.md` §3.1 catalogue) + `docs/godot/resources-and-assets.md` §1.1 (ConfigProvider) + `docs/godot/state-and-signals.md` §1.1 (StateCache) + `docs/gameplay/configuration-and-data.md` §4 (client version caching) + `.instructions/client.md` + `.claude/agents/godot-client.md`. **ConfigProvider = single config read gate** (features never load raw bundles / hardcode gameplay numbers); `config@vN` disk cache is **immutable/write-once** (ADR-005); config changes never rebuild the client. **StateCache = read-only/display-only** (ADR-007) — only `apply_snapshot` (server response) writes, **no authoritative mutator**, reads return copies; every authoritative mutation goes `Feature/UI → NetworkClient → Server → response → StateCache`. Reuse both — do not fork a second config/state cache; do not pull in phase-22 e2e/signed bundles. |
| Changes the client boot flow / UI base (`client/src/ui/*`) | `client/src/ui/base/base_view.gd` (`BaseView`) + `client/src/ui/app_root.*` (main scene) + `client/src/ui/boot/*` (`BootController`/`BootView`/`BootErrorView`) + `client/src/ui/main_hub/*` + `client/tests/ui/*` (gdUnit4 — the behavior contract) + `client/project.godot` (`run/main_scene`) + `docs/godot/ui-architecture.md` §2.1 (BaseView/presenter contract) / §4.1 (boot+error UI) + `docs/godot/scene-architecture.md` §4.2 (boot/app-shell) / §5 (autoload status) + `docs/godot/state-and-signals.md` §4 (any new parser) + `.instructions/client.md` + `.claude/agents/godot-client.md` + root `setup-and-run.md`. **A view MUST NOT reference `NetworkClient`/`HTTPRequest`/`core/net`** (grep guard over `src/ui/**`) — the **presenter** is the only touchpoint (view: data-in `set_data`→`_render`, intent-out signal `intent`). **Intent = local signal + presenter; do NOT add a per-UI-action EventBus event** (catalogue stays closed, §3.1). App-shell: `run/main_scene = app_root` → `SceneRouter.goto(boot)` (router owns every screen). Boot: `/health` = hard gate, `ConfigProvider.check_for_update()` = best-effort (Config Service = phase 21). Error UI shows a **safe** message (no stack leak) + retry (connect once, `_running` guard — no duplicate listeners/requests). Reuse `BaseView`/boot — never let a view call the network, never make boot a self-freeing main scene. New endpoint = add a parser in `core/net/response_parser.gd` + call via NetworkClient from a presenter. |
| Changes combat sim behavior | ADR-011 (if the decision changes) + `docs/gameplay/combat-framework.md` + update golden vectors deliberately |
| Adds/changes a module boundary | `docs/architecture/dependency-graph.md` + the module doc (`docs/backend/`, `docs/godot/`, `docs/gameplay/`) + that folder's `README.md` |
| Changes public behavior of a feature | The module doc + module `README.md`; note in `CHANGELOG.md` |
| Advances a roadmap phase / milestone | `docs/roadmap/README.md` + `docs/audit/bootstrap-audit.md` (status) + root `ROADMAP.md` if the stage list changed |
| Adds/changes a CI stage, deploy, or env | `docs/deployment/ci-cd-pipeline.md` / `release-operations.md` + `.github/workflows/*` + root `DEPLOYMENT.md` if the summary changed |
| Changes a convention (naming/style/git) | `docs/conventions/*` (canonical) — root `STYLE_GUIDE.md` only links it |
| Changes the AI workflow/rules | `docs/ai/*` (canonical) → then reflect in this `.claude/` layer + `.rules/rules.md` |
| Discovers an unresolved question | Append to `docs/mvp/10-open-questions.md` — do not resolve silently |
| Populates/changes an AI dotfolder | That folder's `README.md` Contents index + `AI_GUIDE.md` map if roles changed |

## Derived-doc pointers (keep these as links, not copies)
- Root `README.md`, `ARCHITECTURE.md`, `DECISIONS.md`, `ROADMAP.md`, `TESTING.md`, `DEPLOYMENT.md`,
  `STYLE_GUIDE.md`, `CONTRIBUTING.md`, `SECURITY.md` are **navigational** — they link `docs/`.
- The foundational-decisions table appears in a few places; change the canonical one
  (`docs/README.md`) and ensure the others link rather than restate.

## Definition of Done for a doc-sync pass
Every matrix row the change triggers is updated, the canonical/derived link direction is preserved,
and no dead links were introduced.
