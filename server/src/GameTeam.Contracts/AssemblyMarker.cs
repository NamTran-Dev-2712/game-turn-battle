namespace GameTeam.Contracts;

/// <summary>
/// Điểm neo assembly cho Contracts. DTO/request/response versioned thêm ở phase sau
/// (docs/backend/api-and-versioning.md); là nguồn để sinh model client (shared/codegen).
/// </summary>
public sealed class AssemblyMarker;
