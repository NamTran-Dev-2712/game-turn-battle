# `GameTeam.Contracts` — DTO/hợp đồng versioned

Request/response/DTO dùng cho API, **versioned**; nguồn để sinh model client (`shared/codegen`). Chỉ tham chiếu `Domain` (enum/hằng). **Owner:** Backend + Platform. Thiết kế: `../../../docs/backend/api-and-versioning.md`. **Bootstrap:** chỉ `AssemblyMarker`.
