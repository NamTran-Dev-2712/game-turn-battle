namespace GameTeam.Application;

/// <summary>
/// Điểm neo assembly để MediatR/FluentValidation quét handler/validator
/// (xem <see cref="DependencyInjection"/>). KHÔNG chứa logic.
/// </summary>
public sealed class AssemblyMarker;
