# `Application/Common/` — Dùng chung

`Behaviors/` (pipeline: Validation, Logging, Transaction, Caching, Idempotency) · `Ports/` (interface: `IHeroRepository`, `IConfigProvider`, `IClock`, `IUnitOfWork`…) · `Results/` (Result types). **Bootstrap:** placeholder. Thiết kế: `../../../../docs/backend/domain-and-application.md`.
