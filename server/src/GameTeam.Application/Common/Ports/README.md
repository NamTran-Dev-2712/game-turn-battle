# `Common/Ports/` — Interface (ports)

Interface mà Infrastructure hiện thực (DIP): repository, `IConfigProvider`, `IClock`, `IUnitOfWork`… Application chỉ thấy interface. **Bootstrap:** placeholder.
