# `deploy/docker/` — Dockerfile phụ & tài nguyên image

Dockerfile phụ trợ / cấu hình image ngoài `server/Dockerfile` (ví dụ image tool, migration runner). **Owner:** DevOps. **Bootstrap:** placeholder — thêm khi cần. Không chứa secret.
