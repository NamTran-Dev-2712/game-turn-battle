# `deploy/k8s/` — Kubernetes manifest (tương lai)

Chỗ chừa cho manifest/Helm khi scale ra k8s (`mvp/09` SC — scalability). **Owner:** DevOps. **Bootstrap:** placeholder, chưa có manifest. Chi tiết định hướng: `../../docs/deployment/`.
