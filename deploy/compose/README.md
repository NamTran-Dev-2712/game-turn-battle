# `deploy/compose/` — docker-compose local

`docker-compose.yml`: dựng **Postgres + Redis + API** cho dev/integration test cục bộ. Gọi qua `scripts/dev/up.*`. **Owner:** DevOps/dev-experience. Biến lấy từ `../../.env` (copy `../../.env.example`). Không commit secret thật.
