# `config/liveops/` — Event/season/feature-flag (Post-MVP)

Lịch event, banner rotation, season, feature toggle (có start/end/version). **Owner:** LiveOps. Thiết kế: `../../docs/liveops/`. **Bootstrap:** chỗ chừa, chưa có dữ liệu.
