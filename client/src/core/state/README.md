# `core/state/` — StateCache

Autoload cache trạng thái **đọc-chỉ** phục vụ hiển thị. Nguồn sự thật là server (ADR-007); client chỉ cache. **Owner:** Client core. **Không** coi cache là nguồn quyết định nhạy cảm.
