# `features/battle/` — placeholder

Module trình chiếu trận đấu (render kết quả sim). **Chưa hiện thực**. Kết quả nhạy cảm do server re-sim (ADR-011); client render bằng `src/combat`. Nghiệp vụ: `../../../../docs/gameplay/combat-framework.md`.
