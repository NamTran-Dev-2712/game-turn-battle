# `features/summon/` — placeholder

Module summon/gacha (banner, roll, kết quả). **Chưa hiện thực**. Kết quả roll do **server** quyết (ADR-011); client chỉ hiển thị. Nghiệp vụ: `../../../../docs/gameplay/configuration-and-data.md`, `mvp/03`.
