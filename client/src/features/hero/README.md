# `features/hero/` — placeholder

Module hiển thị/quản lý hero (danh sách, chi tiết, nâng cấp UI). **Chưa hiện thực** (phase Implementation). Nghiệp vụ: `../../../../docs/gameplay/hero-system.md`. Không hardcode stat — dùng config qua `core/config`.
