; export_presets.cfg — PLACEHOLDER (phase bootstrap)
;
; Godot sinh/ghi file này khi bạn cấu hình Export Presets trong editor
; (Project > Export). Giữ file để tài liệu hoá chủ đích export:
;   - Android (ưu tiên trước — mvp/10 DP), landscape.
;   - iOS (sau).
;
; KHÔNG commit keystore/chứng chỉ ký ở đây (dùng GitHub Secrets — xem
; ../docs/deployment/ci-cd-pipeline.md §5). Preset thật sẽ do editor tạo.
