# `scripts/ci/` — Helper cho pipeline

Bọc bước hay dùng trong CI (build/test/validate) để workflow gọn & tái dùng local. **Owner:** Platform. **Bootstrap:** thêm helper khi workflow ổn định. Xem `../../.github/workflows/`, `../../docs/deployment/ci-cd-pipeline.md`.
